/*  Rename your component to something descriptive.
    Update its props type to reflect the props you need.
    Please do not use next/image for images. Use react-cool-img instead.
    Please try to keep additional dependencies to a minimum. */
const CustomComponent = (props: any) => {
  // your custom component here
  return null;
};

export default CustomComponent;
